export default {
    plugins: {
        autoprefixer: {}, // Keep autoprefixer if needed, or remove it too if using lightningcss (which vite uses by default now for some things? No, usually esbuild/postcss)
    },
}
